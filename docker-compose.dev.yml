services:
  dev:
    container_name: jake-website-dev
    image: node:22-alpine
    working_dir: /app
    ports:
      - "4321:4321"
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"

volumes:
  node_modules:
