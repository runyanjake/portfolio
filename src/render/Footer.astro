---
import { site } from '../site.config';

const footerText = site.footer ?? `© ${new Date().getFullYear()} ${site.title}`;
---

<footer class="site-footer">
  <div class="site-footer__inner">
    <p>{footerText}</p>
  </div>
</footer>
