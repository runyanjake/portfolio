---
// Wraps an ordered list authored as ordinary markdown and restyles it as
// a numbered procedure. The <ol> stays in the markup so the list remains
// semantic and readable without CSS.
---

<div class="steps">
  <slot />
</div>
