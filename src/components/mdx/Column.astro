---
// A single cell inside <Columns>. Kept as its own component so authors
// declare column boundaries explicitly rather than relying on how MDX
// happens to group sibling nodes.
---

<div class="columns__col">
  <slot />
</div>
